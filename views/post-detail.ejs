<!DOCTYPE html>
<html lang="en">
<%- include('./components/head', { title: post.title + ' - Ingrid Chemicals' }) %>
<body>
  <%- include('./components/header', { currentPage: 'news' }) %>
  <div class="container mt-4">
    <article>
      <h1><%= post.title %></h1>
      <% if (post.image) { %>
        <img src="/uploads/<%= post.image %>" class="img-fluid mb-3" alt="<%= post.title %>" style="max-width: 100%; height: auto; max-height: 500px; object-fit: cover;">
      <% } %>
      <div class="mb-3">
        <p class="text-muted">By <%= post.author %> on <%= post.createdAt.toDateString() %></p>
        <% if (post.category) { %>
          <p><strong>Category:</strong> <%= post.category.name %></p>
        <% } %>
      </div>
      <div class="post-content"><%= post.content %></div>
    </article>
    <a href="/news" class="btn btn-secondary">Back to News</a>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
